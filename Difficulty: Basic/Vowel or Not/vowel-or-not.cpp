#include <bits/stdc++.h>
using namespace std;

int main() {
    char ch;
    cin >> ch;

    // code here
    if(ch == 'A' || ch == 'E' || ch == 'I' || ch == 'O' || ch == 'U' || ch == 'a' || ch == 'e' || ch == 'i' || ch == 'o' || ch == 'u') cout<<"true";
    else cout<<"false";

    return 0;
}